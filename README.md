# llm-zoomcamp-2026-vector
In Retrieval-Augmented Generation (RAG), vector search is the engine that finds the most relevant information to answer a user's prompt.  Instead of looking for exact keyword matches (like a traditional Google search), it searches by meaning and context.
